/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'education.ti.com', 'flagcdn.com'],
    unoptimized: true,
  },
}

module.exports = nextConfig
